import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.redmorrayne.clearfitness',
  appName: 'clear-fitness',
  webDir: 'dist'
};

export default config;
